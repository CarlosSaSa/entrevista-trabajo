# Ejecución
- Instalar las dependencias con el siguiente comando: pip install -r requirements.txt
- Asegurar que las credenciales de la base de datos esten correctas, estas se encuentran en el directorio ./db/db.py
- Ejecutar el siguiente comando con el entorno virtual activado: uvicorn app:app --reload --port 5002